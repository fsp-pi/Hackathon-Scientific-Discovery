# Copy to ui/.env.local for local-dev mode against scripts/dev/up.sh.
# When VITE_LOCAL_DEV=1 the SPA bypasses Cognito + the event-gate password
# and points at a local FastAPI on http://localhost:8000.

VITE_LOCAL_DEV=1
VITE_API_URL=http://localhost:8000
